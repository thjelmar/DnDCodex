node_modules
dist
dist-ssr
*.local
.DS_Store

# Local environment (Supabase keys, etc.) — never commit real values
.env
.env.*
!.env.example

# Editor / tooling
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code local (machine-specific permissions)
.claude/settings.local.json

# TS build info
node_modules/.tmp
